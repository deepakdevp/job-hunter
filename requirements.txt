# Core dependencies (pinned from working environment)
click==8.1.8
python-dotenv==1.1.0
pyyaml==6.0.2
httpx==0.28.1
beautifulsoup4==4.14.3
jinja2==3.1.6
rich==13.9.4

# Optional: LLM Providers
google-genai==1.14.0
# anthropic>=0.30       # pip install job-hunter[claude]
# openai>=1.0           # pip install job-hunter[openai]

# Optional: Notion sync
# notion-client>=2.0    # pip install job-hunter[notion]

# Optional: PDF generation
# weasyprint>=62        # pip install job-hunter[pdf]

# Optional: Browser automation
# playwright>=1.40      # pip install job-hunter[apply]

# Optional: Job scraping
# python-jobspy>=1.1    # pip install job-hunter[jobspy]
